sudo pip install --upgrade pip
sudo pip install --upgrade tensorflow==2.11.0
sudo pip install -e .
sudo pre-commit install
sudo pip install namex
sudo apt install python3.10-venv
sudo git clone https://github.com/keras-team/keras-core.git && cd keras-core && python pip_build.py --install
